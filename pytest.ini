[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: 합성 이미지 렌더링 + Standard 4-model 계산이 포함된 느린 통합 테스트 (pytest -m "not slow"로 건너뛸 수 있음).
        빠른 배선 확인만 필요하면 test_smoke_pipeline.py(마커 없음, 3D->2D 직접
        사영 + Pinhole/Extended 2모델만)를 대신 참고할 것.
