# 테스트 스위트(tests/) 실행에만 필요. 앱 자체를 쓰는 데는 필요 없음.
-r requirements.txt

pytest>=7.4

# tests/test_rosbag_reader.py는 rosbags가 없으면 자동으로 스킵된다
# (requirements.txt에도 있지만, 개발 환경에서 rosbag 테스트를 반드시
# 돌리고 싶다면 여기서도 명시적으로 보장).
rosbags>=0.9
